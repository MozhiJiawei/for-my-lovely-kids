{
  "name": "red-flower-garden",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.10.0",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "format:check": "prettier . --check",
    "typecheck": "tsc -p packages/domain/tsconfig.json --noEmit && tsc -p apps/api/tsconfig.json --noEmit && tsc -p apps/miniprogram/tsconfig.json --noEmit",
    "test": "vitest run",
    "ci": "eslint . --max-warnings=0 && prettier . --check && tsc -p packages/domain/tsconfig.json --noEmit && tsc -p apps/api/tsconfig.json --noEmit && tsc -p apps/miniprogram/tsconfig.json --noEmit && vitest run",
    "ci:local": "eslint . --max-warnings=0 && prettier . --check && tsc -p packages/domain/tsconfig.json --noEmit && tsc -p apps/api/tsconfig.json --noEmit && tsc -p apps/miniprogram/tsconfig.json --noEmit && vitest run && corepack pnpm run e2e",
    "e2e": "node tests/e2e/miniprogram/devtools-load.mjs",
    "dev:api": "tsx apps/api/src/server.ts",
    "miniprogram:typecheck": "tsc -p apps/miniprogram/tsconfig.json --noEmit",
    "miniprogram:pack-npm": "node apps/miniprogram/scripts/pack-npm.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.1",
    "@types/node": "^22.15.2",
    "eslint": "^9.25.1",
    "miniprogram-automator": "0.12.1",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.31.0",
    "vitest": "^3.1.2"
  }
}
